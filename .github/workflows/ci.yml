name: Continuous Integration

# 1. Triggers (Disparadores): Cuándo se ejecuta el pipeline
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# 2. Jobs: Trabajos o máquinas virtuales requeridas
jobs:
  test:
    name: Pruebas Unitarias y Cobertura
    runs-on: ubuntu-latest  # Máquina virtual con Linux Ubuntu

    steps:
      # Paso A: Clonar el código del repositorio en la máquina virtual
      - name: Descargar código del repositorio
        uses: actions/checkout@v4

      # Paso B: Configurar Python 3.11 en el runner
      - name: Configurar Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      # Paso C: Instalar las dependencias de requirements.txt
      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # Paso D: Ejecutar las pruebas unitarias
      - name: Ejecutar pruebas unitarias con pytest
        run: |
          pytest -v

      # Paso E: Validar cobertura mínima del 80%
      - name: Validar umbral de cobertura (>= 80%)
        run: |
          pytest --cov=main --cov-report=term-missing --cov-fail-under=80